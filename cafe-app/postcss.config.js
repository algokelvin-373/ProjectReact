export default {
  plugins: {
    // Tailwind CSS should be processed as a PostCSS plugin to
    // generate the utility classes. Autoprefixer adds vendor prefixes
    // automatically when needed.
    tailwindcss: {},
    autoprefixer: {},
  },
};